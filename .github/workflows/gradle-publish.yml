# This workflow will build a package using Gradle and then publish it to GitHub packages when a push to the master branch is made

name: Gradle Package

on:
  push:
    branches:
      - master  # Trigger the workflow on push to master branch

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 8
      uses: actions/setup-java@v4
      with:
        java-version: '8'  # Use Java 8
        distribution: 'temurin'
        server-id: github # Value of the distributionManagement/repository/id field of the pom.xml
        settings-path: ${{ github.workspace }} # location for the settings.xml file

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2  # Acción correcta para configurar Gradle
      with:
        gradle-version: '8.8'  # Especificar la versión de Gradle 8.8

    - name: Give gradlew execute permission
      run: chmod +x ./gradlew

    - name: Build with Gradle
      run: ./gradlew build

    - name: Publish to GitHub Packages
      run: ./gradlew publish
      env:
        USERNAME: ${{ github.actor }}
        TOKEN: ${{ secrets.GITHUB_TOKEN }}
